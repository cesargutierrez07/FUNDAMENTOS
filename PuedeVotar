#include <iostream>
using namespace std;

int main()
{
    int edad;
    cout<<"Ingresar la edad: ";
    cin>>edad;
    if (edad>=18)
        cout<<"Puede votar";
    else
        cout<<"No puede votar";

    return 0;
}
