#include <iostream>
using namespace std;

int main()
{
    float a, b, suma;
    cout<<"Escribe el primer numero: ";
    cin>>a;
    cout<<"Escribe el segundo numero: ";
    cin>>b;
    suma = a+b;
    cout<<"La suma es: "<<suma;

    return 0;
}
